# Optional dependencies for ai_scorer fine-tuning and packaging.
# Keep separate from runtime scorer dependencies.

torch==2.8.0
transformers==5.15.0
peft==0.20.0
trl==1.10.0
accelerate==1.10.1
datasets==4.7.0
sentencepiece==0.2.1
