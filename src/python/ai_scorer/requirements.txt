pymongo==4.5.0
redis==5.0.8
protobuf==5.29.4
ollama==0.4.7
watchdog==6.0.0
fastembed==0.8.0
google-generativeai==0.8.5

# Optional fine-tuning dependencies are kept separate to avoid inflating scorer runtime installs.
# Install with: pip install -r src/python/ai_scorer/training/requirements-training.txt
